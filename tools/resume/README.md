# Resume source

This directory is the reproducible source for William's one-page Applied AI
Engineer resume. `artifact.md` records the retained CV visual system,
`resume-content.md` records the approved copy and evidence boundaries, and
`build_resume.py` creates the DOCX.

After a content change, export the DOCX with `export_docx_pdf.py`, run
`qa_resume_artifacts.py` against both final files, and visually inspect a
full-page render before replacing `public/william-lo-channiko-resume.pdf`.

The canonical released DOCX and PDF remain in:

`C:\Users\William\OneDrive\Dokumen\CV dan Pas Foto`
